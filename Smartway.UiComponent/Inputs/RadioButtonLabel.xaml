<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputs="clr-namespace:Smartway.UiComponent.Inputs;assembly=Smartway.UiComponent"
             x:Class="Smartway.UiComponent.Inputs.RadioButtonLabel"
             x:Name="Self" Orientation="Horizontal" MinimumHeightRequest="48">
    <StackLayout.GestureRecognizers>
        <TapGestureRecognizer
            Command="{Binding Source={x:Reference Self}, Path=ToggleRadioButton}"
            NumberOfTapsRequired="1" />
        <TapGestureRecognizer
            Command="{Binding Source={x:Reference Self}, Path=Command}"
            CommandParameter="{Binding Source={x:Reference Self}, Path=CommandParameter}"
            NumberOfTapsRequired="1" />
    </StackLayout.GestureRecognizers>
    <Label Text="{Binding Source={x:Reference Self}, Path=Label}" FontSize="16" HorizontalOptions="FillAndExpand"/>
    <inputs:RadioButton IsChecked="{Binding Source={x:Reference Self}, Path=IsChecked, Mode=TwoWay}" HorizontalOptions="End"/>
</StackLayout>
